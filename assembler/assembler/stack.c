#include "globals.h"
#include <stdio.h>
#include "stack.h"
/*
//word* stackBody[STACK_SIZE];
int counter = 0;


void push(word* lable)
{
	if (counter >= STACK_SIZE)
	{
		fprintf(stderr, "error while pushing to the stack. stack is full\n");
	}
	else
	{
		stackBody[counter++] = lable;
	}
}

word* pop()
{
	if (counter <= 0)
	{
		fprintf(stderr, "error while poping from the stack. stack is empty\n");
	}
	else
	{
		return stackBody[counter--];
	}
}

*/